apiVersion: v1
kind: Pod
metadata:
  name: 0-debug-forward-proxy
spec:
  containers:
  - name: 0-debug-forward-proxy
    image: ghcr.io/rherlt/k8s-docker-nginx-forward-proxy:main
    ports:
    - containerPort: 80